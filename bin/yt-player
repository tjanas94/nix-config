#!/usr/bin/env bash

set -euo pipefail

url=$(rg -IB1 'www.youtube.com/(watch|playlist)' "$HOME/org" | rofi -i -dmenu -p yt-player | perl -pe 's# *##')

if [ -n "$url" ]; then
    "${TERMINAL:-xterm}" -t yt-player -e mpv --no-video --ytdl-raw-options='yes-playlist=' "$url"
fi
